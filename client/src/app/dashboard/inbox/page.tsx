'use client';

import InboxView from '@/components/inbox/InboxView';

export default function InboxPage() {
  // InboxView fills the shell's flex child (overflow: hidden, flex: 1)
  return <InboxView />;
}
