# ============================================================================
# BASE REQUIREMENTS — installed in project-genesis-base image
# Shared by ALL microservices. Pin every version for deterministic caching.
# ============================================================================

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.10.0
pydantic-settings==2.1.0
email-validator==2.3.0

# PostgreSQL (async)
sqlalchemy==2.0.36
asyncpg==0.30.0

# MongoDB (async)
motor==3.3.2
pymongo==4.6.1

# Redis (async)
redis==5.0.1
hiredis==3.1.0

# Celery + broker
celery==5.3.6
kombu==5.3.5

# HTTP clients
httpx==0.26.0
aiohttp==3.11.0

# Auth / security
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
cryptography==42.0.0

# Google OAuth
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
python-dateutil==2.8.2
structlog==24.1.0

# Vector DB client (lightweight — no ML model)
qdrant-client==1.7.1
