# ============================================================================
# ML REQUIREMENTS — installed on top of base image
# Only used by: auth-service, auth-celery-worker
# CPU-only torch to avoid GPU bloat
# ============================================================================

# CPU-only torch — avoids pulling CUDA (~3GB saved vs default)
torch==2.6.0

# Sentence transformers (depends on torch)
sentence-transformers==3.2.0
transformers==4.47.0
