# ============================================================================
# GLOBAL REQUIREMENTS
# All microservices dependencies
# ============================================================================

# ── FastAPI & Web Framework ─────────────────────────────────────────────────
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Database Drivers ────────────────────────────────────────────────────────
# PostgreSQL
sqlalchemy==2.0.25
asyncpg==0.29.0
psycopg2-binary==2.9.9

# MongoDB
motor==3.3.2
pymongo==4.6.1

# ── Redis & Caching ─────────────────────────────────────────────────────────
redis==5.0.1
hiredis==2.3.2

# ── Celery & Task Queue ─────────────────────────────────────────────────────
celery==5.3.6
kombu==5.3.5

# ── HTTP Client ─────────────────────────────────────────────────────────────
httpx==0.26.0
aiohttp==3.9.1

# ── Authentication & Security ───────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
cryptography==42.0.0

# ── Utilities ───────────────────────────────────────────────────────────────
python-dotenv==1.0.0
pyyaml==6.0.1
python-dateutil==2.8.2

# ── Logging & Monitoring ────────────────────────────────────────────────────
structlog==24.1.0

# ── Testing (Optional) ──────────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0

# ── Development Tools ───────────────────────────────────────────────────────
black==24.1.1
flake8==7.0.0
mypy==1.8.0

# ── LLM ─────────────────────────────────────────────────────────────────────
openai>=1.30.0

# ── Vector Database & ML ────────────────────────────────────────────────────
qdrant-client==1.7.0
# sentence-transformers 2.2.2 is incompatible with huggingface_hub >= 0.17.0
# (cached_download was removed). 2.7.0 is the minimum version that works with
# huggingface_hub 0.26.x, torch 2.1.2, and transformers 4.36.2.
sentence-transformers==2.7.0
torch==2.1.2
transformers==4.36.2
# tokenizers must stay < 0.19 — transformers 4.36.2 requires tokenizers<0.19,>=0.14
# tokenizers >= 0.16 writes tokenizer.json with list-of-lists merges format that
# tokenizers 0.15.x cannot read. Pin to 0.15.2 which matches the cached model format.
tokenizers==0.15.2
# huggingface_hub must be >= 0.17.0 (cached_download removed) and compatible with
# sentence-transformers 2.7.0 and transformers 4.36.2
huggingface_hub>=0.20.0,<0.27.0
# numpy must stay < 2.0 — torch 2.1.2 is not numpy 2.x compatible
numpy>=1.24.0,<2.0.0
# Required by sentence-transformers cosine similarity utilities
scipy>=1.11.0
# NLI model used by intent engine: cross-encoder/nli-distilroberta-base (190MB, cached)
# If tokenizer.json fails with 'ModelWrapper' error, run: python server/_fix_tokenizer.py